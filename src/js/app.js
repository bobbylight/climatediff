var climatediff = angular.module('cdApp',
   [ 'ui.bootstrap', 'angularSpinner', 'cdControllers', 'cdDirectives' ]);
   //[ 'gcControllers', 'gcDirectives', 'gcServices', 'pasvaz.bindonce' ]);

/*
gameCollector.config(['$routeProvider', function($routeProvider) {
   'use strict';
   
   $routeProvider.when('/', {
      templateUrl: 'partials/home.html',
      controller:  'MainPageCtrl'
   }).
   when('/about', {
      templateUrl: 'partials/about.html',
      controller: 'MainPageCtrl'
   }).
   when('/nes', {
      templateUrl: 'partials/allGames.html',
      controller:  'AllGamesCtrl'
   }).
   when('/myGames', {
      templateUrl: 'partials/myGames.html',
      controller: 'MyGamesCtrl'
   });
   
}]);
*/

function init() {
   
//   $(document).ready(function() {
//      $('form#cityForm').submit(function(e) {
//         e.preventDefault(); // Stop actual form submit
////         createChart($scope);
//      });
//   });
   
}
